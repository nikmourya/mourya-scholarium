# -- Mourya Scholarium API -- Python Dependencies --
# Render-compatible: heavy ML/NLP libs (spacy, sklearn, xgboost, etc.) removed
# because they fail to compile on Render free tier.
# None of them are actually imported by any Python module at runtime.

# Core
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1

# Database
sqlalchemy==2.0.30
asyncpg==0.29.0
aiosqlite==0.20.0
alembic==1.13.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# HTTP / APIs
httpx==0.27.0
aiohttp==3.9.5

# Utilities
uuid6==2024.1.12
python-dateutil==2.9.0
