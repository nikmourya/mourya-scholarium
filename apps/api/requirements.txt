# -- Mourya Scholarium API -- Python Dependencies --
# Render free-tier compatible (Python 3.11, pre-built wheels only).
# Pinned after audit of all actual runtime imports across agents/ and routers/.

# Core
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
pydantic-settings==2.3.4
python-dotenv==1.0.1

# Database (SQLite mode via USE_SQLITE=true)
sqlalchemy==2.0.30
aiosqlite==0.20.0
alembic==1.13.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# HTTP / APIs
httpx==0.27.0
aiohttp==3.9.5

# AI (used in agents/writing.py)
anthropic==0.28.0
