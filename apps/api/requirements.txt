# -- Mourya Scholarium API -- Python Dependencies --
# Render free-tier compatible. Versions chosen to use pre-built binary wheels.
# No packages that require compilation (Rust, C++) at install time.

# Core
fastapi==0.111.0
uvicorn[standard]==0.30.1
# pydantic v2 with explicit pydantic-core wheel (no Rust compilation needed)
pydantic==2.7.1
pydantic-settings==2.3.4
python-dotenv==1.0.1

# Database
sqlalchemy==2.0.30
aiosqlite==0.20.0
alembic==1.13.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# HTTP / APIs
httpx==0.27.0
aiohttp==3.9.5
