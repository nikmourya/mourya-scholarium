# Mourya Scholarium — Frontend Environment Variables
# Copy this to .env.local for local overrides.

# Backend API URL — leave empty for local dev (uses Next.js proxy rewrite).
# For production, set to your deployed backend URL, e.g.:
#   NEXT_PUBLIC_API_URL=https://mourya-scholarium-api.onrender.com
NEXT_PUBLIC_API_URL=
